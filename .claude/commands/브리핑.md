사용자가 `/브리핑` 명령어를 실행했습니다.

인수: $ARGUMENTS

## 처리 방법

인수는 종목 티커 심볼입니다 (예: RKT, AAPL, 005930.KS).

1. `C:/Users/asdf/webtest/auto_post.py` 파일을 읽어서 현재 `ANALYSIS_TICKERS` 딕셔너리를 확인하세요.

2. 아래 Python 코드로 yfinance에서 종목 이름을 조회하세요:
   ```
   cd C:/Users/asdf/webtest && python -X utf8 -c "import yfinance as yf; t = yf.Ticker('$ARGUMENTS'); info = t.info; print(info.get('longName') or info.get('shortName') or '$ARGUMENTS')"
   ```
   - 조회 실패 시 티커 심볼을 이름으로 사용하세요.

3. `auto_post.py`의 `ANALYSIS_TICKERS` 블록을 새 종목으로 교체하세요:
   ```python
   ANALYSIS_TICKERS = {
       '조회한 종목명': '입력한 티커',
   }
   ```

4. 아래 명령어로 git commit + push 하세요:
   ```
   cd C:/Users/asdf/webtest && git add auto_post.py && git commit -m "change analysis ticker: 이전티커 → 새티커" && git push
   ```

5. 완료 메시지를 한국어로 출력하세요. 예: "✅ 특별 분석 종목이 RKT (Rocket Companies)로 변경됐어요."
